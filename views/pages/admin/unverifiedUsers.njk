<!DOCTYPE html>
<html lang="se">
<head>
    {% include "../../templates/defaultHeadTags.njk" %}
    <link rel="stylesheet" type="text/css" href="/assets/css/forms.css">
    <link rel="stylesheet" type="text/css" href="/assets/css/explore.css">
    <link rel="stylesheet" type="text/css" href="/assets/css/admin.css">
    <script src="/assets/scripts/unverifiedUsers.js" type="module" defer></script>
    <title>Admin - PictureIt</title>
</head>
<body>
    <header>
        Verifiera Användare
    </header>

    <div class="users">
        {% for user in users %}
            <div class="user" userID="{{ user._id }}" id="{{ user.name }}">
                <div class="user-pfp" id="{{ user.name }}"></div>
                <p class="username">{{ user.name }}</p>
                <button class="verify" id="{{ user._id }}">Verifiera</button>
                <button class="delete" id="{{ user._id }}">Ta Bort</button>
            </div>
        {% endfor %}
        {% if not users[0] %}
            <p>Finns inga icke-verifierade användare.</p>
        {% endif %}
    </div>
</body>
</html>